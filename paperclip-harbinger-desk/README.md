# Harbinger Desk — Paperclip package

Give this folder to Paperclip (Agent Companies `agentcompanies/v1`).

```sh
npx companies.sh add ./paperclip-harbinger-desk --include company,agents,projects,tasks,skills --target new --new-company-name "Harbinger Desk"
```

Agents land paused. Activate Hermes first.
